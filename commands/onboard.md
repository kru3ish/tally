---
description: One-page report from the Claude Code transcripts already on this machine: spend, biggest sessions, waste, one habit to change, plan comparison (no model calls)
argument-hint: [--since 30d]
allowed-tools: Bash(node *)
---

```!
node "${CLAUDE_PLUGIN_ROOT}/dist/cli.js" onboard $ARGUMENTS
```

Show the user the report above and point at the one habit line.
